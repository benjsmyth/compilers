/* A program to compute the factorial value of an integer */

void main(void) {
  int x; 
  int fac;
  bool test;

  test = true;

  x = input();
  fac = 1;
  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}
