/* Syntactic/Semantic Errors
    - Missing Index for Array
    - Invalid Argument list for Function
    - Missing Semicolon
*/

int gcd(int u, int v) {
  if (v == 0) return u;
  else return gcd(v, u - u/v*v);
}

void main(void) {
  int x;  
  int y

  int arr[5];
  x = arr[];

  gcd(x, );
}
